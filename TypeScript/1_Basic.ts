let num1: number=10;
let num2: number=20;

function sum(a :number , b:number)
{
    return (a+b);
}

console.log(sum(num1, num2))

// data_type syntax --> let variable : data_type = value;
//compile : tsc file_name